# Implement Feature Prompt

Read `AGENTS.md`, `docs/SYSTEM.md`, `docs/SECURITY.md`, and `audit/MIGRATION_MAP.md`.

Task: implement a scoped feature using external pinned OAI source. Store changes as feature-separated patches under the appropriate `patches/` subdirectory, keep generated configs and secrets out of Git, validate against the rollback baseline, and document evidence.
