# Deprecated Firecell Donor DU Local-F1 Template
#
# The current Quectel F1 backhaul architecture does not use a firecell donor DU.
# It uses a monolithic firecell donor gNB for the Quectel modem and a separate
# CU/access-DU split over wg-quectel-f1 for the minipc access cell.
#
# This placeholder is retained only to document that the old local-F1 donor-DU
# path must not be launched accidentally. Runtime helpers should fail closed if
# they encounter this path.
#
# Supported donor config:
#   /home/serber/monolithic/openairinterface5g/targets/PROJECTS/GENERIC-NR-5GC/CONF/gnb-firecell-donor-single-core-51prb.conf
#
# Supported launch:
#   ./scripts/oai-lab-tui --start-caged-quectel
